/**
 * 小程序配置文件
 */

// 主机域名
const host = 'http://api.tianapi.com'
const config = {
  // 下面为具体Api地址
  host,
  requestUrl: `cbd1acd103a210a39190a1e90f38679e`,
  imgUrl: `https://${host}`
}

module.exports = config